name: 🐍 Generate Contribution Snake Animation

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-snake:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate Snake Animations
        uses: Platane/snk@v3.3.0
        with:
          github_user_name: Rajath2005
          github_token: ${{ secrets.GITHUB_TOKEN }}
          outputs: |
            assets/snake-dark.svg?palette=github-dark&color_snake=#00FFD1&color_dots=#0D1117,#161B22,#21262D,#30363D,#484F58
            assets/snake-light.svg?palette=github&color_snake=#00897B&color_dots=#ebedf0,#9be9a8,#40c463,#30a14e,#216e39

      - name: Commit and Push
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add assets/snake-*.svg
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "🐍 Update snake animations [$(date +'%Y-%m-%d')]"
            git push
          fi
